/* global AbortController, ApexCharts, WebSocket, ResizeObserver */
import { auth, db } from "/js/lib/firebase.js";
import { MARKET_DATA_API_BASE } from "/js/config/market-data-config.js?v=investment-market-live-data-1";
import { onAuthStateChanged } from "https://www.gstatic.com/firebasejs/9.23.0/firebase-auth.js";
import { doc, getDoc } from "https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore.js";

const REST_BASE="https://data-api.binance.vision", WS_BASE="wss://stream.binance.com:9443/ws", LIMIT=300, FAVORITES_KEY="teknoify_investment_favorites_v1";
const ASSETS=[{symbol:"BTCUSDT",display:"BTC-USDT",name:"Bitcoin",tr:"Bitcoin",icon:"B"},{symbol:"ETHUSDT",display:"ETH-USDT",name:"Ethereum",tr:"Ethereum",icon:"E"},{symbol:"BNBUSDT",display:"BNB-USDT",name:"BNB",tr:"BNB",icon:"N"},{symbol:"SOLUSDT",display:"SOL-USDT",name:"Solana",tr:"Solana",icon:"S"},{symbol:"XRPUSDT",display:"XRP-USDT",name:"XRP",tr:"XRP",icon:"X"},{symbol:"ADAUSDT",display:"ADA-USDT",name:"Cardano",tr:"Cardano",icon:"A"},{symbol:"AVAXUSDT",display:"AVAX-USDT",name:"Avalanche",tr:"Avalanche",icon:"V"},{symbol:"DOGEUSDT",display:"DOGE-USDT",name:"Dogecoin",tr:"Dogecoin",icon:"D"}];
const UPCOMING_ASSETS=[{symbol:"AAPL",display:"AAPL",name:"Apple",tr:"Apple",icon:"A"},{symbol:"TSLA",display:"TSLA",name:"Tesla",tr:"Tesla",icon:"T"},{symbol:"GLD",display:"GLD",name:"Gold ETF",tr:"Altın ETF",icon:"G"},{symbol:"EURUSD",display:"EUR/USD",name:"Euro / Dollar",tr:"Euro / Dolar",icon:"€"},{symbol:"AMZN",display:"Amazon",name:"Amazon",tr:"Amazon",icon:"A"}];
const OVERVIEW_IDS=["BIST100","SP500","BTCUSD"], STATUS_LABELS={live:"Canlı",delayed:"Gecikmeli",eod:"Gün Sonu",manual:"Manuel",stale:"Son Veri",upcoming:"Yakında",unavailable:"Bağlantı Yok",connecting:"Bağlanıyor"};
const state={selected:ASSETS[0],interval:"1h",candles:[],lastRealCandles:[],priceChart:null,volumeChart:null,rsiChart:null,macdChart:null,overviewCharts:new Map(),ws:null,generation:0,retries:0,reconnectTimer:null,pollTimer:null,watchTimer:null,watchInFlight:false,watchBackoff:30000,favorites:new Set(),filter:"all",query:"",hidden:false,unloading:false,overviewInFlight:false,lastOverviewFetch:0,overviewBackoff:60000,overviewRetryAfter:0,lastHiddenAt:0};
const $=id=>document.getElementById(id), $$=s=>document.querySelectorAll(s); const money=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",minimumFractionDigits:2,maximumFractionDigits:2}); const smallMoney=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",minimumFractionDigits:4,maximumFractionDigits:6}); const pctFmt=new Intl.NumberFormat("en-US",{minimumFractionDigits:2,maximumFractionDigits:2,signDisplay:"always"}); const trTime=new Intl.DateTimeFormat("tr-TR",{timeZone:"Europe/Istanbul",dateStyle:"short",timeStyle:"short"});
function safeNum(v){const n=Number(v);return Number.isFinite(n)?n:null} function fmtPrice(n){return Number.isFinite(n)?(Math.abs(n)<1?smallMoney:money).format(n):"—"} function fmtValue(n,a={}){if(!Number.isFinite(n))return"—"; if(a.valueType==="index")return new Intl.NumberFormat("tr-TR",{maximumFractionDigits:2}).format(n); if(a.currency==="USD"||a.valueType==="currency"||a.valueType==="price")return fmtPrice(n); return new Intl.NumberFormat("tr-TR",{maximumFractionDigits:2}).format(n)} function fmtPct(n){return Number.isFinite(n)?`${pctFmt.format(n)}%`:"—"} function changeClass(n){return n>0?"positive":n<0?"negative":"neutral"} function setText(id,v){const el=$(id); if(el)el.textContent=v} function setStatus(kind,text){const el=$("market-status"); if(!el)return; el.className=`market-status ${kind}`; el.replaceChildren(document.createElement("span"),document.createTextNode(text));}
function overviewDefaults(status="upcoming"){return new Map(OVERVIEW_IDS.map(id=>[id,{id,label:id==="BIST100"?"BIST 100":id==="SP500"?"S&P 500":"BTC/USD",subtitle:id==="BIST100"?"Borsa İstanbul":id==="SP500"?"ABD Büyük Şirketler Endeksi":"Bitcoin",price:null,changePercent:null,status,statusLabel:STATUS_LABELS[status],points:[],valueType:id==="BTCUSD"?"price":"index",currency:id==="BTCUSD"?"USD":null,lastUpdatedAt:null}]))}
function validPoints(points){return (Array.isArray(points)?points:[]).map(p=>({x:safeNum(p.timestamp),y:safeNum(p.value)})).filter(p=>Number.isFinite(p.x)&&Number.isFinite(p.y)).sort((a,b)=>a.x-b.x)}
function renderOverviewAsset(asset){const card=document.querySelector(`[data-overview-card="${asset.id}"]`); if(!card)return; card.querySelector("[data-overview-title]").textContent=asset.label||asset.id; card.querySelector("[data-overview-subtitle]").textContent=asset.subtitle||"—"; card.querySelector("[data-overview-price]").textContent=fmtValue(safeNum(asset.price),asset); const change=card.querySelector("[data-overview-change]"); change.textContent=fmtPct(safeNum(asset.changePercent)); change.className=`overview-change ${changeClass(safeNum(asset.changePercent))}`; const status=asset.status||"upcoming"; const badge=card.querySelector("[data-overview-status]"); badge.textContent=asset.statusLabel||STATUS_LABELS[status]||"Yakında"; badge.className=`freshness-badge ${status}`; card.querySelector("[data-overview-updated]").textContent=asset.lastUpdatedAt?`Son güncelleme: ${trTime.format(new Date(asset.lastUpdatedAt))}`:"—"; const points=validPoints(asset.points); const shell=card.querySelector(".overview-chart-shell"); shell.classList.remove("loading"); if(points.length<2){destroyOverviewChart(asset.id); card.classList.remove("has-chart"); card.querySelector(".overview-empty").textContent=status==="unavailable"?"Piyasa verileri şu anda yüklenemiyor.":"Veri entegrasyonu hazırlanıyor."; return} const direction=points.at(-1).y-points[0].y; createOrUpdateOverviewChart(asset.id,points,direction); card.classList.add("has-chart")}
function createOrUpdateOverviewChart(assetId,points,direction){const ids={BIST100:"overview-chart-bist100",SP500:"overview-chart-sp500",BTCUSD:"overview-chart-btcusd"}; const el=$(ids[assetId]); if(!el)return; const tone=direction>0?"#22c55e":direction<0?"#f43f5e":"#818cf8"; const options={chart:{type:"area",height:114,sparkline:{enabled:true},background:"transparent",animations:{enabled:false},toolbar:{show:false}},theme:{mode:"dark"},series:[{name:assetId,data:points}],stroke:{curve:"smooth",width:2.5,colors:[tone]},fill:{type:"gradient",gradient:{shadeIntensity:.2,opacityFrom:.32,opacityTo:.03,stops:[0,90,100],colorStops:[]}},colors:[tone],grid:{show:false},xaxis:{type:"datetime",labels:{show:false},axisTicks:{show:false},axisBorder:{show:false}},yaxis:{labels:{show:false}},tooltip:{enabled:true,theme:"dark",x:{formatter:v=>trTime.format(new Date(v))},y:{formatter:v=>fmtValue(Number(v),{valueType:assetId==="BTCUSD"?"price":"index",currency:assetId==="BTCUSD"?"USD":null})}}}; const current=state.overviewCharts.get(assetId); if(current)current.updateOptions(options,false,true); else{const chart=new ApexCharts(el,options); state.overviewCharts.set(assetId,chart); chart.render();}}
function destroyOverviewChart(assetId){const c=state.overviewCharts.get(assetId); if(c){c.destroy(); state.overviewCharts.delete(assetId)}}
function renderOverviewMap(map){OVERVIEW_IDS.forEach(id=>renderOverviewAsset(map.get(id)||overviewDefaults().get(id)))}
async function fetchOverview(retryToken=false,force=false){if(state.hidden||state.overviewInFlight)return; const now=Date.now(); if(!force&&(now-state.lastOverviewFetch<60000||now<state.overviewRetryAfter))return; if(!MARKET_DATA_API_BASE){const m=overviewDefaults("upcoming"); updateBtcOverviewFromSession(m); renderOverviewMap(m); return} state.overviewInFlight=true; state.lastOverviewFetch=now; try{const user=auth.currentUser; if(!user)throw new Error("No authenticated user"); const ctl=new AbortController(); const timer=setTimeout(()=>ctl.abort(),10000); const token=await user.getIdToken(retryToken); const res=await fetch(`${MARKET_DATA_API_BASE}/v1/market/overview`,{headers:{Authorization:`Bearer ${token}`},signal:ctl.signal}); clearTimeout(timer); if(res.status===401&&!retryToken)return fetchOverview(true,true); if(res.status===429){const ra=Number(res.headers.get("Retry-After")); state.overviewRetryAfter=Date.now()+(Number.isFinite(ra)?ra*1000:state.overviewBackoff); state.overviewBackoff=Math.min(state.overviewBackoff*2,300000); throw new Error("Rate limited")} if(!res.ok)throw new Error(`Overview HTTP ${res.status}`); const json=await res.json(); const m=overviewDefaults(); (json.assets||[]).forEach(a=>m.set(a.id,a)); renderOverviewMap(m); state.overviewBackoff=60000}catch(e){console.warn("Piyasa özeti yüklenemedi",e); const m=overviewDefaults("upcoming"); updateBtcOverviewFromSession(m); renderOverviewMap(m)}finally{state.overviewInFlight=false}}
function updateBtcOverviewFromSession(map=overviewDefaults()){if(state.lastRealCandles.length>=2){const first=state.lastRealCandles[0],last=state.lastRealCandles.at(-1); const change=((last.close-first.close)/first.close)*100; map.set("BTCUSD",{...map.get("BTCUSD"),price:last.close,changePercent:change,status:"live",statusLabel:"Canlı",lastUpdatedAt:new Date(last.timestamp).toISOString(),points:state.lastRealCandles.slice(-72).map(c=>({timestamp:c.timestamp,value:c.close})),valueType:"price",currency:"USD"});}else map.set("BTCUSD",{...map.get("BTCUSD"),status:"unavailable",statusLabel:"Bağlantı Yok"});}
function loadFavs(){try{state.favorites=new Set(JSON.parse(localStorage.getItem(FAVORITES_KEY)||"[]").filter(s=>ASSETS.concat(UPCOMING_ASSETS).some(a=>a.symbol===s)))}catch{state.favorites=new Set()}} function saveFavs(){localStorage.setItem(FAVORITES_KEY,JSON.stringify([...state.favorites]))}
async function authInit(){onAuthStateChanged(auth,async user=>{if(!user){location.href="/";return} let userData={}; try{const s=await getDoc(doc(db,"users",user.uid)); if(s.exists()){const rootData=s.data()||{}; userData=rootData.data||rootData||{}}}catch(e){console.warn("Profil okunamadı",e)} const name=userData.displayName||userData.name||user.displayName||user.email||"Teknoify Kullanıcısı"; const photo=userData.profile?.photoURL||userData.photoURL||user.photoURL||""; window.USER_SESSION={uid:user.uid,email:user.email,displayName:name,photoURL:photo,isAdmin:!!userData.isAdmin}; renderProfile(name,photo); if(typeof window.TK_RENDER_SIDEBAR==="function")window.TK_RENDER_SIDEBAR(); startMarket();});}
function renderProfile(name,photo){setText("investment-user-name",name); const av=$("investment-user-avatar"); if(!av)return; av.textContent=""; if(photo){const img=document.createElement("img"); img.src=photo; img.alt=""; av.appendChild(img)}else av.textContent=(name.trim()[0]||"T").toUpperCase(); $("investment-profile-button")?.addEventListener("click",()=>document.querySelector(".user-profile,.profile-trigger")?.click());}
function candleFromRow(r){if(!Array.isArray(r)||r.length<6)return null; const c={timestamp:safeNum(r[0]),open:safeNum(r[1]),high:safeNum(r[2]),low:safeNum(r[3]),close:safeNum(r[4]),volume:safeNum(r[5])}; return [c.timestamp,c.open,c.high,c.low,c.close,c.volume].every(Number.isFinite)?c:null} function normalize(rows){const map=new Map(); rows.map(candleFromRow).filter(Boolean).forEach(c=>map.set(c.timestamp,c)); return [...map.values()].sort((a,b)=>a.timestamp-b.timestamp).slice(-LIMIT)}
async function fetchCandles(symbol=state.selected.symbol,interval=state.interval){const ctl=new AbortController(); const t=setTimeout(()=>ctl.abort(),10000); try{const res=await fetch(`${REST_BASE}/api/v3/klines?symbol=${encodeURIComponent(symbol)}&interval=${encodeURIComponent(interval)}&limit=${LIMIT}`,{signal:ctl.signal}); if(!res.ok)throw new Error(`Kline HTTP ${res.status}`); const json=await res.json(); if(!Array.isArray(json))throw new Error("Malformed kline payload"); const candles=normalize(json); if(!candles.length)throw new Error("Empty kline data"); return candles}finally{clearTimeout(t)}}
function smaData(){return state.candles.map((c,i)=>{if(i<19)return{x:c.timestamp,y:null}; const avg=state.candles.slice(i-19,i+1).reduce((sum,v)=>sum+v.close,0)/20; return{x:c.timestamp,y:Number(avg.toFixed(8))}})} function ema(values,period){const k=2/(period+1); let prev=null; return values.map((v,i)=>{prev=i===0?v:(v*k+prev*(1-k)); return prev})} function rsiData(period=14){let gains=0,losses=0; return state.candles.map((c,i)=>{if(i===0)return{x:c.timestamp,y:null}; const diff=c.close-state.candles[i-1].close; if(i<=period){gains+=Math.max(diff,0); losses+=Math.max(-diff,0); if(i<period)return{x:c.timestamp,y:null};}else{gains=(gains*(period-1)+Math.max(diff,0))/period; losses=(losses*(period-1)+Math.max(-diff,0))/period;} const rsi=losses===0?100:100-(100/(1+gains/losses)); return{x:c.timestamp,y:Number(rsi.toFixed(2))}})} function macdSeries(){const closes=state.candles.map(c=>c.close),fast=ema(closes,12),slow=ema(closes,26),macd=closes.map((_,i)=>fast[i]-slow[i]),signal=ema(macd,9); return[{name:"MACD",type:"line",data:state.candles.map((c,i)=>({x:c.timestamp,y:i<26?null:Number(macd[i].toFixed(6))}))},{name:"Signal",type:"line",data:state.candles.map((c,i)=>({x:c.timestamp,y:i<35?null:Number(signal[i].toFixed(6))}))},{name:"Histogram",type:"bar",data:state.candles.map((c,i)=>({x:c.timestamp,y:i<35?null:Number((macd[i]-signal[i]).toFixed(6)),fillColor:macd[i]>=signal[i]?"#10b981":"#ef4444"}))}]} function priceSeries(){return[{name:"Fiyat",type:"candlestick",data:state.candles.map(c=>({x:c.timestamp,y:[c.open,c.high,c.low,c.close]}))},{name:"SMA 20",type:"line",data:smaData()}]} function volumeSeries(){return[{name:"Hacim",data:state.candles.map(c=>({x:c.timestamp,y:c.volume,fillColor:c.close>=c.open?"#10b981":"#ef4444"}))}]}
function chartOpts(){return{chart:{id:"investment-price-chart",type:"candlestick",height:360,background:"transparent",animations:{enabled:false},toolbar:{show:false},zoom:{enabled:true}},theme:{mode:"dark"},grid:{borderColor:"rgba(148,163,184,.10)",strokeDashArray:3},xaxis:{type:"datetime",labels:{style:{colors:"#7f8ea3"},datetimeUTC:false,formatter:v=>trTime.format(new Date(v))}},yaxis:{opposite:true,labels:{style:{colors:"#93a4b8"},formatter:v=>fmtPrice(v)}},tooltip:{theme:"dark",x:{formatter:v=>trTime.format(new Date(v))}},plotOptions:{candlestick:{colors:{upward:"#22c55e",downward:"#f43f5e"},wick:{useFillColor:true}}},stroke:{width:[1,2],colors:[undefined,"#a78bfa"]},series:priceSeries()}} function volOpts(){return{chart:{id:"investment-volume-chart",type:"bar",height:96,background:"transparent",animations:{enabled:false},toolbar:{show:false},zoom:{enabled:false}},theme:{mode:"dark"},grid:{borderColor:"rgba(148,163,184,.08)"},plotOptions:{bar:{columnWidth:"68%"}},xaxis:{type:"datetime",labels:{show:false}},yaxis:{labels:{style:{colors:"#7f8ea3"},formatter:v=>Intl.NumberFormat("en-US",{notation:"compact"}).format(v)}},tooltip:{theme:"dark",x:{formatter:v=>trTime.format(new Date(v))}},series:volumeSeries()}} function rsiOpts(){return{chart:{id:"investment-rsi-chart",type:"line",height:92,background:"transparent",animations:{enabled:false},toolbar:{show:false}},theme:{mode:"dark"},grid:{borderColor:"rgba(148,163,184,.08)"},stroke:{width:2,curve:"smooth",colors:["#38bdf8"]},xaxis:{type:"datetime",labels:{show:false}},yaxis:{min:0,max:100,tickAmount:2,labels:{style:{colors:"#7f8ea3"}}},tooltip:{theme:"dark"},series:[{name:"RSI 14",data:rsiData()}]}} function macdOpts(){return{chart:{id:"investment-macd-chart",height:110,background:"transparent",animations:{enabled:false},toolbar:{show:false}},theme:{mode:"dark"},grid:{borderColor:"rgba(148,163,184,.08)"},stroke:{width:[2,2,0],curve:"smooth",colors:["#60a5fa","#f59e0b",undefined]},plotOptions:{bar:{columnWidth:"65%"}},xaxis:{type:"datetime",labels:{show:false}},yaxis:{labels:{style:{colors:"#7f8ea3"}}},tooltip:{theme:"dark"},series:macdSeries()}}
async function renderCharts(){if(!state.priceChart){state.priceChart=new ApexCharts($("investment-price-chart"),chartOpts()); state.volumeChart=new ApexCharts($("investment-volume-chart"),volOpts()); state.rsiChart=new ApexCharts($("investment-rsi-chart"),rsiOpts()); state.macdChart=new ApexCharts($("investment-macd-chart"),macdOpts()); await Promise.all([state.priceChart.render(),state.volumeChart.render(),state.rsiChart.render(),state.macdChart.render()])}else{await state.priceChart.updateSeries(priceSeries(),false); await state.volumeChart.updateSeries(volumeSeries(),false); await state.rsiChart.updateSeries([{name:"RSI 14",data:rsiData()}],false); await state.macdChart.updateSeries(macdSeries(),false)} ["price-chart-card","volume-chart-card","rsi-chart-card","macd-chart-card"].forEach(id=>$(id)?.classList.remove("loading"));}
function updateSelected(price,change){setText("selected-display-symbol",state.selected.display); setText("selected-asset-name",state.selected.name); setText("selected-asset-icon",state.selected.icon); setText("selected-current-price",fmtPrice(price)); const el=$("selected-change"); if(el){el.textContent=fmtPct(change); el.className=`change-value ${changeClass(change)}`;} setText("chart-selected-symbol",state.selected.display.replace("-","/")); setText("chart-live-price",fmtPrice(price)); document.title=`${state.selected.display} | Yatırım | Teknoify`;}
async function loadSymbol(){state.generation++; const gen=state.generation; closeSocket(false); clearPolling(); setStatus("connecting","Bağlanıyor"); $("chart-message").hidden=true; ["price-chart-card","volume-chart-card","rsi-chart-card","macd-chart-card"].forEach(id=>$(id)?.classList.add("loading")); updateSelected(NaN,NaN); try{state.candles=await fetchCandles(); state.lastRealCandles=state.candles.slice(); if(gen!==state.generation)return; await renderCharts(); updateSelected(state.candles.at(-1)?.close,getWatchChange(state.selected.symbol)); if(state.selected.symbol==="BTCUSDT")fetchOverview(false,true); openSocket(gen)}catch(e){console.error("Kline yüklenemedi",e); const msg=$("chart-message"); msg.hidden=false; msg.querySelector("span").textContent=state.lastRealCandles.length?`Son gerçek veri gösteriliyor: ${trTime.format(new Date(state.lastRealCandles.at(-1).timestamp))}`:"Piyasa verileri şu anda yüklenemiyor."; if(state.lastRealCandles.length){state.candles=state.lastRealCandles.slice(); await renderCharts(); setStatus("stale","Son Veri");}else{["price-chart-card","volume-chart-card","rsi-chart-card","macd-chart-card"].forEach(id=>$(id)?.classList.remove("loading")); setStatus("unavailable","Bağlantı Yok"); const m=overviewDefaults(); updateBtcOverviewFromSession(m); renderOverviewMap(m);}}}
function openSocket(gen=state.generation){if(state.hidden||state.unloading)return; closeSocket(false); const ws=new WebSocket(`${WS_BASE}/${state.selected.symbol.toLowerCase()}@kline_${state.interval}`); state.ws=ws; ws.onopen=()=>{if(gen!==state.generation)return; state.retries=0; setStatus("live","Canlı")}; ws.onmessage=ev=>{if(gen!==state.generation)return; try{const msg=JSON.parse(ev.data),k=msg.k; if(!k||k.s!==state.selected.symbol||k.i!==state.interval)return; const c={timestamp:safeNum(k.t),open:safeNum(k.o),high:safeNum(k.h),low:safeNum(k.l),close:safeNum(k.c),volume:safeNum(k.v)}; if(![c.timestamp,c.open,c.high,c.low,c.close,c.volume].every(Number.isFinite))return; upsertCandle(c); state.lastRealCandles=state.candles.slice(); updateSelected(c.close,getWatchChange(state.selected.symbol)); renderCharts(); if(state.selected.symbol==="BTCUSDT"&&!MARKET_DATA_API_BASE){const m=overviewDefaults(); updateBtcOverviewFromSession(m); renderOverviewMap(m)}}catch(e){console.warn("WS mesajı işlenemedi",e)}}; ws.onerror=()=>setStatus("connecting","Bağlanıyor"); ws.onclose=()=>{if(gen!==state.generation||state.unloading)return; scheduleReconnect(gen)}}
function upsertCandle(c){const i=state.candles.findIndex(x=>x.timestamp===c.timestamp); if(i>=0)state.candles[i]=c; else state.candles.push(c); state.candles.sort((a,b)=>a.timestamp-b.timestamp); state.candles=state.candles.slice(-LIMIT)} function closeSocket(increment=true){if(increment)state.generation++; if(state.ws){state.ws.onclose=null; state.ws.close(); state.ws=null} if(state.reconnectTimer){clearTimeout(state.reconnectTimer); state.reconnectTimer=null}} function scheduleReconnect(gen){if(state.reconnectTimer)return; state.retries++; if(state.retries>5){startPolling();return} setStatus("connecting","Bağlanıyor"); state.reconnectTimer=setTimeout(()=>{state.reconnectTimer=null; openSocket(gen)},Math.min(30000,1000*2**(state.retries-1)))} function startPolling(){clearPolling(); setStatus("delayed","Gecikmeli"); state.pollTimer=setInterval(async()=>{if(state.hidden)return; try{state.candles=await fetchCandles(); state.lastRealCandles=state.candles.slice(); await renderCharts(); updateSelected(state.candles.at(-1)?.close,getWatchChange(state.selected.symbol))}catch(e){console.warn("Polling başarısız",e); if(state.lastRealCandles.length)setStatus("stale","Son Veri"); else setStatus("unavailable","Bağlantı Yok")}},12000)} function clearPolling(){if(state.pollTimer)clearInterval(state.pollTimer); state.pollTimer=null}
const watchData=new Map(); function getWatchChange(s){return watchData.get(s)?.change} function renderWatchlist(){const rows=$("watchlist-rows"); rows.textContent=""; const q=state.query.trim().toLowerCase(); const all=[ASSETS[1],...UPCOMING_ASSETS,...ASSETS.filter(a=>a.symbol!=="ETHUSDT")]; const items=all.filter(a=>(state.filter==="all"||state.favorites.has(a.symbol))&&(!q||[a.display,a.symbol,a.name,a.tr].some(v=>v.toLowerCase().includes(q)))); if(!items.length){const p=document.createElement("p"); p.className="empty-state"; p.textContent=state.filter==="favorites"&&!state.favorites.size?"Henüz favori varlığınız yok.":"Sonuç bulunamadı."; rows.appendChild(p); return} items.forEach(a=>{const upcoming=UPCOMING_ASSETS.some(x=>x.symbol===a.symbol); const row=document.createElement("button"); row.type="button"; row.className=`watch-row ${upcoming?"is-upcoming":""}`; row.dataset.symbol=a.symbol; row.setAttribute("aria-current",String(a.symbol===state.selected.symbol)); row.title=upcoming?"Bu varlığın canlı veri entegrasyonu yakında eklenecek.":""; row.innerHTML=`<span class="asset-token"></span><span><strong></strong><span class="watch-symbol"></span></span><span class="watch-price"><strong></strong><span></span></span>`; row.children[0].textContent=a.icon; row.children[1].querySelector("strong").textContent=a.name; row.children[1].querySelector(".watch-symbol").textContent=upcoming?`${a.display} · Yakında`:a.display; const d=watchData.get(a.symbol)||{}; row.children[2].querySelector("strong").textContent=upcoming?"—":fmtPrice(d.price); const ch=row.children[2].querySelector("span"); ch.textContent=upcoming?"Yakında":fmtPct(d.change); ch.className=upcoming?"neutral":changeClass(d.change); const fav=document.createElement("button"); fav.type="button"; fav.className=`favorite-button ${state.favorites.has(a.symbol)?"is-favorite":""}`; fav.setAttribute("aria-label",`${a.display} favorisini ${state.favorites.has(a.symbol)?"kaldır":"ekle"}`); fav.innerHTML='<i class="fas fa-star"></i>'; fav.onclick=e=>{e.stopPropagation(); state.favorites.has(a.symbol)?state.favorites.delete(a.symbol):state.favorites.add(a.symbol); saveFavs(); renderWatchlist()}; row.appendChild(fav); row.onclick=()=>{if(upcoming)return; state.selected=a; renderWatchlist(); loadSymbol()}; rows.appendChild(row)})}
async function updateWatchlist(){if(state.watchInFlight||state.hidden)return; state.watchInFlight=true; const err=$("watchlist-error"); try{const symbols=encodeURIComponent(JSON.stringify(ASSETS.map(a=>a.symbol))); const res=await fetch(`${REST_BASE}/api/v3/ticker/24hr?symbols=${symbols}`); if(res.status===429){state.watchBackoff=Math.min(state.watchBackoff*2,240000); throw new Error("Rate limited")} if(!res.ok)throw new Error(`Ticker HTTP ${res.status}`); const json=await res.json(); if(!Array.isArray(json))throw new Error("Malformed ticker"); json.forEach(t=>{const a=ASSETS.find(x=>x.symbol===t.symbol),price=safeNum(t.lastPrice),change=safeNum(t.priceChangePercent); if(a&&Number.isFinite(price)&&Number.isFinite(change))watchData.set(a.symbol,{price,change})}); state.watchBackoff=30000; err.hidden=true; setText("watchlist-updated",trTime.format(new Date())); updateSelected(watchData.get(state.selected.symbol)?.price??state.candles.at(-1)?.close,getWatchChange(state.selected.symbol)); renderWatchlist()}catch(e){console.warn("Watchlist güncellenemedi",e); err.hidden=false}finally{state.watchInFlight=false; scheduleWatchlist()}} function scheduleWatchlist(){clearTimeout(state.watchTimer); state.watchTimer=setTimeout(updateWatchlist,state.watchBackoff)}
function bind(){loadFavs(); renderWatchlist(); renderOverviewMap(overviewDefaults("upcoming")); $("watchlist-search").addEventListener("input",e=>{state.query=e.target.value; renderWatchlist()}); $("favorite-filter-toggle")?.addEventListener("click",e=>{state.filter=state.filter==="favorites"?"all":"favorites"; e.currentTarget.classList.toggle("active",state.filter==="favorites"); e.currentTarget.setAttribute("aria-pressed",String(state.filter==="favorites")); renderWatchlist()}); $$(".interval-controls button").forEach(b=>{b.setAttribute("aria-pressed",String(b.classList.contains("active"))); b.onclick=()=>{if(state.interval===b.dataset.interval)return; state.interval=b.dataset.interval; $$(".interval-controls button").forEach(x=>{x.classList.toggle("active",x===b); x.setAttribute("aria-pressed",String(x===b))}); const adv=$("advanced-interval"); if(adv)adv.value=state.interval; loadSymbol()}}); $("advanced-interval")?.addEventListener("change",e=>{state.interval=e.target.value; loadSymbol()}); $("retry-market-load").onclick=()=>{loadSymbol(); fetchOverview(false,true)}; const ro=new ResizeObserver(()=>{state.priceChart?.resize(); state.volumeChart?.resize(); state.rsiChart?.resize(); state.macdChart?.resize(); state.overviewCharts.forEach(c=>c.resize())}); ro.observe(document.querySelector(".investment-dashboard-grid")); window.addEventListener("pagehide",cleanup); window.addEventListener("beforeunload",cleanup); document.addEventListener("visibilitychange",()=>{state.hidden=document.hidden; if(state.hidden){state.lastHiddenAt=Date.now(); closeSocket(); clearPolling(); clearTimeout(state.watchTimer)}else{loadSymbol(); scheduleWatchlist(); if(Date.now()-state.lastHiddenAt>60000)fetchOverview(false,true)}}); window.addEventListener("unload",()=>ro.disconnect())}
function cleanup(){state.unloading=true; closeSocket(); clearPolling(); clearTimeout(state.watchTimer); state.priceChart?.destroy(); state.volumeChart?.destroy(); state.rsiChart?.destroy(); state.macdChart?.destroy(); state.overviewCharts.forEach(c=>c.destroy()); state.overviewCharts.clear()} function startMarket(){bind(); updateSelected(NaN,NaN); fetchOverview(false,true); loadSymbol(); updateWatchlist()} authInit();
