# ─── Ortam Değişkenleri (.env.example) ────────────────────────────────────────
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun.
# .env dosyasını asla git'e commit etmeyin.
#
# cp .env.example .env

GCP_PROJECT=teknoify-9449c          # GCP Project ID (Firebase ile aynı)
BQ_DATASET=teknoify_scraping        # BigQuery dataset adı
BQ_TABLE=price_comparisons          # Ana tablo adı
MAX_ROWS=10000                       # Tek sorguda maksimum satır
ALLOWED_ORIGINS=https://teknoify.com,https://www.teknoify.com
# Geliştirme için: ALLOWED_ORIGINS=*
