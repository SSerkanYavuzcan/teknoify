(function () {
    function init() {
        // Retained as a harmless compatibility shim for validation commands.
    }

    function selectDemo() {
        // Static previews are rendered directly by demo-catalog.js.
    }

    window.TeknoifySandboxSimulator = {
        init,
        selectDemo
    };
})();
